// Code generated by ent, DO NOT EDIT.

package genclients

import (
	"context"
	"fmcam/systems/genclients/fmalertgroup"
	"fmcam/systems/genclients/predicate"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// FmAlertGroupQuery is the builder for querying FmAlertGroup entities.
type FmAlertGroupQuery struct {
	config
	ctx        *QueryContext
	order      []fmalertgroup.OrderOption
	inters     []Interceptor
	predicates []predicate.FmAlertGroup
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the FmAlertGroupQuery builder.
func (fagq *FmAlertGroupQuery) Where(ps ...predicate.FmAlertGroup) *FmAlertGroupQuery {
	fagq.predicates = append(fagq.predicates, ps...)
	return fagq
}

// Limit the number of records to be returned by this query.
func (fagq *FmAlertGroupQuery) Limit(limit int) *FmAlertGroupQuery {
	fagq.ctx.Limit = &limit
	return fagq
}

// Offset to start from.
func (fagq *FmAlertGroupQuery) Offset(offset int) *FmAlertGroupQuery {
	fagq.ctx.Offset = &offset
	return fagq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (fagq *FmAlertGroupQuery) Unique(unique bool) *FmAlertGroupQuery {
	fagq.ctx.Unique = &unique
	return fagq
}

// Order specifies how the records should be ordered.
func (fagq *FmAlertGroupQuery) Order(o ...fmalertgroup.OrderOption) *FmAlertGroupQuery {
	fagq.order = append(fagq.order, o...)
	return fagq
}

// First returns the first FmAlertGroup entity from the query.
// Returns a *NotFoundError when no FmAlertGroup was found.
func (fagq *FmAlertGroupQuery) First(ctx context.Context) (*FmAlertGroup, error) {
	nodes, err := fagq.Limit(1).All(setContextOp(ctx, fagq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{fmalertgroup.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (fagq *FmAlertGroupQuery) FirstX(ctx context.Context) *FmAlertGroup {
	node, err := fagq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first FmAlertGroup ID from the query.
// Returns a *NotFoundError when no FmAlertGroup ID was found.
func (fagq *FmAlertGroupQuery) FirstID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = fagq.Limit(1).IDs(setContextOp(ctx, fagq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{fmalertgroup.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (fagq *FmAlertGroupQuery) FirstIDX(ctx context.Context) int64 {
	id, err := fagq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single FmAlertGroup entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one FmAlertGroup entity is found.
// Returns a *NotFoundError when no FmAlertGroup entities are found.
func (fagq *FmAlertGroupQuery) Only(ctx context.Context) (*FmAlertGroup, error) {
	nodes, err := fagq.Limit(2).All(setContextOp(ctx, fagq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{fmalertgroup.Label}
	default:
		return nil, &NotSingularError{fmalertgroup.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (fagq *FmAlertGroupQuery) OnlyX(ctx context.Context) *FmAlertGroup {
	node, err := fagq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only FmAlertGroup ID in the query.
// Returns a *NotSingularError when more than one FmAlertGroup ID is found.
// Returns a *NotFoundError when no entities are found.
func (fagq *FmAlertGroupQuery) OnlyID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = fagq.Limit(2).IDs(setContextOp(ctx, fagq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{fmalertgroup.Label}
	default:
		err = &NotSingularError{fmalertgroup.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (fagq *FmAlertGroupQuery) OnlyIDX(ctx context.Context) int64 {
	id, err := fagq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of FmAlertGroups.
func (fagq *FmAlertGroupQuery) All(ctx context.Context) ([]*FmAlertGroup, error) {
	ctx = setContextOp(ctx, fagq.ctx, ent.OpQueryAll)
	if err := fagq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*FmAlertGroup, *FmAlertGroupQuery]()
	return withInterceptors[[]*FmAlertGroup](ctx, fagq, qr, fagq.inters)
}

// AllX is like All, but panics if an error occurs.
func (fagq *FmAlertGroupQuery) AllX(ctx context.Context) []*FmAlertGroup {
	nodes, err := fagq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of FmAlertGroup IDs.
func (fagq *FmAlertGroupQuery) IDs(ctx context.Context) (ids []int64, err error) {
	if fagq.ctx.Unique == nil && fagq.path != nil {
		fagq.Unique(true)
	}
	ctx = setContextOp(ctx, fagq.ctx, ent.OpQueryIDs)
	if err = fagq.Select(fmalertgroup.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (fagq *FmAlertGroupQuery) IDsX(ctx context.Context) []int64 {
	ids, err := fagq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (fagq *FmAlertGroupQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, fagq.ctx, ent.OpQueryCount)
	if err := fagq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, fagq, querierCount[*FmAlertGroupQuery](), fagq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (fagq *FmAlertGroupQuery) CountX(ctx context.Context) int {
	count, err := fagq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (fagq *FmAlertGroupQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, fagq.ctx, ent.OpQueryExist)
	switch _, err := fagq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("genclients: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (fagq *FmAlertGroupQuery) ExistX(ctx context.Context) bool {
	exist, err := fagq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the FmAlertGroupQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (fagq *FmAlertGroupQuery) Clone() *FmAlertGroupQuery {
	if fagq == nil {
		return nil
	}
	return &FmAlertGroupQuery{
		config:     fagq.config,
		ctx:        fagq.ctx.Clone(),
		order:      append([]fmalertgroup.OrderOption{}, fagq.order...),
		inters:     append([]Interceptor{}, fagq.inters...),
		predicates: append([]predicate.FmAlertGroup{}, fagq.predicates...),
		// clone intermediate query.
		sql:  fagq.sql.Clone(),
		path: fagq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		GroupName string `json:"group_name"  db:"group_name"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.FmAlertGroup.Query().
//		GroupBy(fmalertgroup.FieldGroupName).
//		Aggregate(genclients.Count()).
//		Scan(ctx, &v)
func (fagq *FmAlertGroupQuery) GroupBy(field string, fields ...string) *FmAlertGroupGroupBy {
	fagq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &FmAlertGroupGroupBy{build: fagq}
	grbuild.flds = &fagq.ctx.Fields
	grbuild.label = fmalertgroup.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		GroupName string `json:"group_name"  db:"group_name"`
//	}
//
//	client.FmAlertGroup.Query().
//		Select(fmalertgroup.FieldGroupName).
//		Scan(ctx, &v)
func (fagq *FmAlertGroupQuery) Select(fields ...string) *FmAlertGroupSelect {
	fagq.ctx.Fields = append(fagq.ctx.Fields, fields...)
	sbuild := &FmAlertGroupSelect{FmAlertGroupQuery: fagq}
	sbuild.label = fmalertgroup.Label
	sbuild.flds, sbuild.scan = &fagq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a FmAlertGroupSelect configured with the given aggregations.
func (fagq *FmAlertGroupQuery) Aggregate(fns ...AggregateFunc) *FmAlertGroupSelect {
	return fagq.Select().Aggregate(fns...)
}

func (fagq *FmAlertGroupQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range fagq.inters {
		if inter == nil {
			return fmt.Errorf("genclients: uninitialized interceptor (forgotten import genclients/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, fagq); err != nil {
				return err
			}
		}
	}
	for _, f := range fagq.ctx.Fields {
		if !fmalertgroup.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("genclients: invalid field %q for query", f)}
		}
	}
	if fagq.path != nil {
		prev, err := fagq.path(ctx)
		if err != nil {
			return err
		}
		fagq.sql = prev
	}
	return nil
}

func (fagq *FmAlertGroupQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*FmAlertGroup, error) {
	var (
		nodes = []*FmAlertGroup{}
		_spec = fagq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*FmAlertGroup).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &FmAlertGroup{config: fagq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, fagq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (fagq *FmAlertGroupQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := fagq.querySpec()
	_spec.Node.Columns = fagq.ctx.Fields
	if len(fagq.ctx.Fields) > 0 {
		_spec.Unique = fagq.ctx.Unique != nil && *fagq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, fagq.driver, _spec)
}

func (fagq *FmAlertGroupQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(fmalertgroup.Table, fmalertgroup.Columns, sqlgraph.NewFieldSpec(fmalertgroup.FieldID, field.TypeInt64))
	_spec.From = fagq.sql
	if unique := fagq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if fagq.path != nil {
		_spec.Unique = true
	}
	if fields := fagq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, fmalertgroup.FieldID)
		for i := range fields {
			if fields[i] != fmalertgroup.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := fagq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := fagq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := fagq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := fagq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (fagq *FmAlertGroupQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(fagq.driver.Dialect())
	t1 := builder.Table(fmalertgroup.Table)
	columns := fagq.ctx.Fields
	if len(columns) == 0 {
		columns = fmalertgroup.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if fagq.sql != nil {
		selector = fagq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if fagq.ctx.Unique != nil && *fagq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range fagq.predicates {
		p(selector)
	}
	for _, p := range fagq.order {
		p(selector)
	}
	if offset := fagq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := fagq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// FmAlertGroupGroupBy is the group-by builder for FmAlertGroup entities.
type FmAlertGroupGroupBy struct {
	selector
	build *FmAlertGroupQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (faggb *FmAlertGroupGroupBy) Aggregate(fns ...AggregateFunc) *FmAlertGroupGroupBy {
	faggb.fns = append(faggb.fns, fns...)
	return faggb
}

// Scan applies the selector query and scans the result into the given value.
func (faggb *FmAlertGroupGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, faggb.build.ctx, ent.OpQueryGroupBy)
	if err := faggb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*FmAlertGroupQuery, *FmAlertGroupGroupBy](ctx, faggb.build, faggb, faggb.build.inters, v)
}

func (faggb *FmAlertGroupGroupBy) sqlScan(ctx context.Context, root *FmAlertGroupQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(faggb.fns))
	for _, fn := range faggb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*faggb.flds)+len(faggb.fns))
		for _, f := range *faggb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*faggb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := faggb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// FmAlertGroupSelect is the builder for selecting fields of FmAlertGroup entities.
type FmAlertGroupSelect struct {
	*FmAlertGroupQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (fags *FmAlertGroupSelect) Aggregate(fns ...AggregateFunc) *FmAlertGroupSelect {
	fags.fns = append(fags.fns, fns...)
	return fags
}

// Scan applies the selector query and scans the result into the given value.
func (fags *FmAlertGroupSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, fags.ctx, ent.OpQuerySelect)
	if err := fags.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*FmAlertGroupQuery, *FmAlertGroupSelect](ctx, fags.FmAlertGroupQuery, fags, fags.inters, v)
}

func (fags *FmAlertGroupSelect) sqlScan(ctx context.Context, root *FmAlertGroupQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(fags.fns))
	for _, fn := range fags.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*fags.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := fags.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
