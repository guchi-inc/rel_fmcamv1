// Code generated by ent, DO NOT EDIT.

package genclients

import (
	"context"
	"fmcam/systems/genclients/govcity"
	"fmcam/systems/genclients/predicate"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// GovCityQuery is the builder for querying GovCity entities.
type GovCityQuery struct {
	config
	ctx        *QueryContext
	order      []govcity.OrderOption
	inters     []Interceptor
	predicates []predicate.GovCity
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the GovCityQuery builder.
func (gcq *GovCityQuery) Where(ps ...predicate.GovCity) *GovCityQuery {
	gcq.predicates = append(gcq.predicates, ps...)
	return gcq
}

// Limit the number of records to be returned by this query.
func (gcq *GovCityQuery) Limit(limit int) *GovCityQuery {
	gcq.ctx.Limit = &limit
	return gcq
}

// Offset to start from.
func (gcq *GovCityQuery) Offset(offset int) *GovCityQuery {
	gcq.ctx.Offset = &offset
	return gcq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (gcq *GovCityQuery) Unique(unique bool) *GovCityQuery {
	gcq.ctx.Unique = &unique
	return gcq
}

// Order specifies how the records should be ordered.
func (gcq *GovCityQuery) Order(o ...govcity.OrderOption) *GovCityQuery {
	gcq.order = append(gcq.order, o...)
	return gcq
}

// First returns the first GovCity entity from the query.
// Returns a *NotFoundError when no GovCity was found.
func (gcq *GovCityQuery) First(ctx context.Context) (*GovCity, error) {
	nodes, err := gcq.Limit(1).All(setContextOp(ctx, gcq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{govcity.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (gcq *GovCityQuery) FirstX(ctx context.Context) *GovCity {
	node, err := gcq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first GovCity ID from the query.
// Returns a *NotFoundError when no GovCity ID was found.
func (gcq *GovCityQuery) FirstID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = gcq.Limit(1).IDs(setContextOp(ctx, gcq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{govcity.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (gcq *GovCityQuery) FirstIDX(ctx context.Context) int64 {
	id, err := gcq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single GovCity entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one GovCity entity is found.
// Returns a *NotFoundError when no GovCity entities are found.
func (gcq *GovCityQuery) Only(ctx context.Context) (*GovCity, error) {
	nodes, err := gcq.Limit(2).All(setContextOp(ctx, gcq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{govcity.Label}
	default:
		return nil, &NotSingularError{govcity.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (gcq *GovCityQuery) OnlyX(ctx context.Context) *GovCity {
	node, err := gcq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only GovCity ID in the query.
// Returns a *NotSingularError when more than one GovCity ID is found.
// Returns a *NotFoundError when no entities are found.
func (gcq *GovCityQuery) OnlyID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = gcq.Limit(2).IDs(setContextOp(ctx, gcq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{govcity.Label}
	default:
		err = &NotSingularError{govcity.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (gcq *GovCityQuery) OnlyIDX(ctx context.Context) int64 {
	id, err := gcq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of GovCities.
func (gcq *GovCityQuery) All(ctx context.Context) ([]*GovCity, error) {
	ctx = setContextOp(ctx, gcq.ctx, ent.OpQueryAll)
	if err := gcq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*GovCity, *GovCityQuery]()
	return withInterceptors[[]*GovCity](ctx, gcq, qr, gcq.inters)
}

// AllX is like All, but panics if an error occurs.
func (gcq *GovCityQuery) AllX(ctx context.Context) []*GovCity {
	nodes, err := gcq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of GovCity IDs.
func (gcq *GovCityQuery) IDs(ctx context.Context) (ids []int64, err error) {
	if gcq.ctx.Unique == nil && gcq.path != nil {
		gcq.Unique(true)
	}
	ctx = setContextOp(ctx, gcq.ctx, ent.OpQueryIDs)
	if err = gcq.Select(govcity.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (gcq *GovCityQuery) IDsX(ctx context.Context) []int64 {
	ids, err := gcq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (gcq *GovCityQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, gcq.ctx, ent.OpQueryCount)
	if err := gcq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, gcq, querierCount[*GovCityQuery](), gcq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (gcq *GovCityQuery) CountX(ctx context.Context) int {
	count, err := gcq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (gcq *GovCityQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, gcq.ctx, ent.OpQueryExist)
	switch _, err := gcq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("genclients: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (gcq *GovCityQuery) ExistX(ctx context.Context) bool {
	exist, err := gcq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the GovCityQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (gcq *GovCityQuery) Clone() *GovCityQuery {
	if gcq == nil {
		return nil
	}
	return &GovCityQuery{
		config:     gcq.config,
		ctx:        gcq.ctx.Clone(),
		order:      append([]govcity.OrderOption{}, gcq.order...),
		inters:     append([]Interceptor{}, gcq.inters...),
		predicates: append([]predicate.GovCity{}, gcq.predicates...),
		// clone intermediate query.
		sql:  gcq.sql.Clone(),
		path: gcq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Code string `json:"code"  db:"code"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.GovCity.Query().
//		GroupBy(govcity.FieldCode).
//		Aggregate(genclients.Count()).
//		Scan(ctx, &v)
func (gcq *GovCityQuery) GroupBy(field string, fields ...string) *GovCityGroupBy {
	gcq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &GovCityGroupBy{build: gcq}
	grbuild.flds = &gcq.ctx.Fields
	grbuild.label = govcity.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Code string `json:"code"  db:"code"`
//	}
//
//	client.GovCity.Query().
//		Select(govcity.FieldCode).
//		Scan(ctx, &v)
func (gcq *GovCityQuery) Select(fields ...string) *GovCitySelect {
	gcq.ctx.Fields = append(gcq.ctx.Fields, fields...)
	sbuild := &GovCitySelect{GovCityQuery: gcq}
	sbuild.label = govcity.Label
	sbuild.flds, sbuild.scan = &gcq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a GovCitySelect configured with the given aggregations.
func (gcq *GovCityQuery) Aggregate(fns ...AggregateFunc) *GovCitySelect {
	return gcq.Select().Aggregate(fns...)
}

func (gcq *GovCityQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range gcq.inters {
		if inter == nil {
			return fmt.Errorf("genclients: uninitialized interceptor (forgotten import genclients/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, gcq); err != nil {
				return err
			}
		}
	}
	for _, f := range gcq.ctx.Fields {
		if !govcity.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("genclients: invalid field %q for query", f)}
		}
	}
	if gcq.path != nil {
		prev, err := gcq.path(ctx)
		if err != nil {
			return err
		}
		gcq.sql = prev
	}
	return nil
}

func (gcq *GovCityQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*GovCity, error) {
	var (
		nodes = []*GovCity{}
		_spec = gcq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*GovCity).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &GovCity{config: gcq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, gcq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (gcq *GovCityQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := gcq.querySpec()
	_spec.Node.Columns = gcq.ctx.Fields
	if len(gcq.ctx.Fields) > 0 {
		_spec.Unique = gcq.ctx.Unique != nil && *gcq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, gcq.driver, _spec)
}

func (gcq *GovCityQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(govcity.Table, govcity.Columns, sqlgraph.NewFieldSpec(govcity.FieldID, field.TypeInt64))
	_spec.From = gcq.sql
	if unique := gcq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if gcq.path != nil {
		_spec.Unique = true
	}
	if fields := gcq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, govcity.FieldID)
		for i := range fields {
			if fields[i] != govcity.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := gcq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := gcq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := gcq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := gcq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (gcq *GovCityQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(gcq.driver.Dialect())
	t1 := builder.Table(govcity.Table)
	columns := gcq.ctx.Fields
	if len(columns) == 0 {
		columns = govcity.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if gcq.sql != nil {
		selector = gcq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if gcq.ctx.Unique != nil && *gcq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range gcq.predicates {
		p(selector)
	}
	for _, p := range gcq.order {
		p(selector)
	}
	if offset := gcq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := gcq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// GovCityGroupBy is the group-by builder for GovCity entities.
type GovCityGroupBy struct {
	selector
	build *GovCityQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (gcgb *GovCityGroupBy) Aggregate(fns ...AggregateFunc) *GovCityGroupBy {
	gcgb.fns = append(gcgb.fns, fns...)
	return gcgb
}

// Scan applies the selector query and scans the result into the given value.
func (gcgb *GovCityGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, gcgb.build.ctx, ent.OpQueryGroupBy)
	if err := gcgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*GovCityQuery, *GovCityGroupBy](ctx, gcgb.build, gcgb, gcgb.build.inters, v)
}

func (gcgb *GovCityGroupBy) sqlScan(ctx context.Context, root *GovCityQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(gcgb.fns))
	for _, fn := range gcgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*gcgb.flds)+len(gcgb.fns))
		for _, f := range *gcgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*gcgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := gcgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// GovCitySelect is the builder for selecting fields of GovCity entities.
type GovCitySelect struct {
	*GovCityQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (gcs *GovCitySelect) Aggregate(fns ...AggregateFunc) *GovCitySelect {
	gcs.fns = append(gcs.fns, fns...)
	return gcs
}

// Scan applies the selector query and scans the result into the given value.
func (gcs *GovCitySelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, gcs.ctx, ent.OpQuerySelect)
	if err := gcs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*GovCityQuery, *GovCitySelect](ctx, gcs.GovCityQuery, gcs, gcs.inters, v)
}

func (gcs *GovCitySelect) sqlScan(ctx context.Context, root *GovCityQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(gcs.fns))
	for _, fn := range gcs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*gcs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := gcs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
