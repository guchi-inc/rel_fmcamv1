// Code generated by ent, DO NOT EDIT.

package genclients

import (
	"context"
	"fmcam/systems/genclients/grouprofiletypemapping"
	"fmcam/systems/genclients/predicate"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// GrouProfileTypeMappingQuery is the builder for querying GrouProfileTypeMapping entities.
type GrouProfileTypeMappingQuery struct {
	config
	ctx        *QueryContext
	order      []grouprofiletypemapping.OrderOption
	inters     []Interceptor
	predicates []predicate.GrouProfileTypeMapping
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the GrouProfileTypeMappingQuery builder.
func (gptmq *GrouProfileTypeMappingQuery) Where(ps ...predicate.GrouProfileTypeMapping) *GrouProfileTypeMappingQuery {
	gptmq.predicates = append(gptmq.predicates, ps...)
	return gptmq
}

// Limit the number of records to be returned by this query.
func (gptmq *GrouProfileTypeMappingQuery) Limit(limit int) *GrouProfileTypeMappingQuery {
	gptmq.ctx.Limit = &limit
	return gptmq
}

// Offset to start from.
func (gptmq *GrouProfileTypeMappingQuery) Offset(offset int) *GrouProfileTypeMappingQuery {
	gptmq.ctx.Offset = &offset
	return gptmq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (gptmq *GrouProfileTypeMappingQuery) Unique(unique bool) *GrouProfileTypeMappingQuery {
	gptmq.ctx.Unique = &unique
	return gptmq
}

// Order specifies how the records should be ordered.
func (gptmq *GrouProfileTypeMappingQuery) Order(o ...grouprofiletypemapping.OrderOption) *GrouProfileTypeMappingQuery {
	gptmq.order = append(gptmq.order, o...)
	return gptmq
}

// First returns the first GrouProfileTypeMapping entity from the query.
// Returns a *NotFoundError when no GrouProfileTypeMapping was found.
func (gptmq *GrouProfileTypeMappingQuery) First(ctx context.Context) (*GrouProfileTypeMapping, error) {
	nodes, err := gptmq.Limit(1).All(setContextOp(ctx, gptmq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{grouprofiletypemapping.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (gptmq *GrouProfileTypeMappingQuery) FirstX(ctx context.Context) *GrouProfileTypeMapping {
	node, err := gptmq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first GrouProfileTypeMapping ID from the query.
// Returns a *NotFoundError when no GrouProfileTypeMapping ID was found.
func (gptmq *GrouProfileTypeMappingQuery) FirstID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = gptmq.Limit(1).IDs(setContextOp(ctx, gptmq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{grouprofiletypemapping.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (gptmq *GrouProfileTypeMappingQuery) FirstIDX(ctx context.Context) int64 {
	id, err := gptmq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single GrouProfileTypeMapping entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one GrouProfileTypeMapping entity is found.
// Returns a *NotFoundError when no GrouProfileTypeMapping entities are found.
func (gptmq *GrouProfileTypeMappingQuery) Only(ctx context.Context) (*GrouProfileTypeMapping, error) {
	nodes, err := gptmq.Limit(2).All(setContextOp(ctx, gptmq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{grouprofiletypemapping.Label}
	default:
		return nil, &NotSingularError{grouprofiletypemapping.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (gptmq *GrouProfileTypeMappingQuery) OnlyX(ctx context.Context) *GrouProfileTypeMapping {
	node, err := gptmq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only GrouProfileTypeMapping ID in the query.
// Returns a *NotSingularError when more than one GrouProfileTypeMapping ID is found.
// Returns a *NotFoundError when no entities are found.
func (gptmq *GrouProfileTypeMappingQuery) OnlyID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = gptmq.Limit(2).IDs(setContextOp(ctx, gptmq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{grouprofiletypemapping.Label}
	default:
		err = &NotSingularError{grouprofiletypemapping.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (gptmq *GrouProfileTypeMappingQuery) OnlyIDX(ctx context.Context) int64 {
	id, err := gptmq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of GrouProfileTypeMappings.
func (gptmq *GrouProfileTypeMappingQuery) All(ctx context.Context) ([]*GrouProfileTypeMapping, error) {
	ctx = setContextOp(ctx, gptmq.ctx, ent.OpQueryAll)
	if err := gptmq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*GrouProfileTypeMapping, *GrouProfileTypeMappingQuery]()
	return withInterceptors[[]*GrouProfileTypeMapping](ctx, gptmq, qr, gptmq.inters)
}

// AllX is like All, but panics if an error occurs.
func (gptmq *GrouProfileTypeMappingQuery) AllX(ctx context.Context) []*GrouProfileTypeMapping {
	nodes, err := gptmq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of GrouProfileTypeMapping IDs.
func (gptmq *GrouProfileTypeMappingQuery) IDs(ctx context.Context) (ids []int64, err error) {
	if gptmq.ctx.Unique == nil && gptmq.path != nil {
		gptmq.Unique(true)
	}
	ctx = setContextOp(ctx, gptmq.ctx, ent.OpQueryIDs)
	if err = gptmq.Select(grouprofiletypemapping.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (gptmq *GrouProfileTypeMappingQuery) IDsX(ctx context.Context) []int64 {
	ids, err := gptmq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (gptmq *GrouProfileTypeMappingQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, gptmq.ctx, ent.OpQueryCount)
	if err := gptmq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, gptmq, querierCount[*GrouProfileTypeMappingQuery](), gptmq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (gptmq *GrouProfileTypeMappingQuery) CountX(ctx context.Context) int {
	count, err := gptmq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (gptmq *GrouProfileTypeMappingQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, gptmq.ctx, ent.OpQueryExist)
	switch _, err := gptmq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("genclients: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (gptmq *GrouProfileTypeMappingQuery) ExistX(ctx context.Context) bool {
	exist, err := gptmq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the GrouProfileTypeMappingQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (gptmq *GrouProfileTypeMappingQuery) Clone() *GrouProfileTypeMappingQuery {
	if gptmq == nil {
		return nil
	}
	return &GrouProfileTypeMappingQuery{
		config:     gptmq.config,
		ctx:        gptmq.ctx.Clone(),
		order:      append([]grouprofiletypemapping.OrderOption{}, gptmq.order...),
		inters:     append([]Interceptor{}, gptmq.inters...),
		predicates: append([]predicate.GrouProfileTypeMapping{}, gptmq.predicates...),
		// clone intermediate query.
		sql:  gptmq.sql.Clone(),
		path: gptmq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		GroupID int64 `json:"group_id" db:"group_id"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.GrouProfileTypeMapping.Query().
//		GroupBy(grouprofiletypemapping.FieldGroupID).
//		Aggregate(genclients.Count()).
//		Scan(ctx, &v)
func (gptmq *GrouProfileTypeMappingQuery) GroupBy(field string, fields ...string) *GrouProfileTypeMappingGroupBy {
	gptmq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &GrouProfileTypeMappingGroupBy{build: gptmq}
	grbuild.flds = &gptmq.ctx.Fields
	grbuild.label = grouprofiletypemapping.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		GroupID int64 `json:"group_id" db:"group_id"`
//	}
//
//	client.GrouProfileTypeMapping.Query().
//		Select(grouprofiletypemapping.FieldGroupID).
//		Scan(ctx, &v)
func (gptmq *GrouProfileTypeMappingQuery) Select(fields ...string) *GrouProfileTypeMappingSelect {
	gptmq.ctx.Fields = append(gptmq.ctx.Fields, fields...)
	sbuild := &GrouProfileTypeMappingSelect{GrouProfileTypeMappingQuery: gptmq}
	sbuild.label = grouprofiletypemapping.Label
	sbuild.flds, sbuild.scan = &gptmq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a GrouProfileTypeMappingSelect configured with the given aggregations.
func (gptmq *GrouProfileTypeMappingQuery) Aggregate(fns ...AggregateFunc) *GrouProfileTypeMappingSelect {
	return gptmq.Select().Aggregate(fns...)
}

func (gptmq *GrouProfileTypeMappingQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range gptmq.inters {
		if inter == nil {
			return fmt.Errorf("genclients: uninitialized interceptor (forgotten import genclients/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, gptmq); err != nil {
				return err
			}
		}
	}
	for _, f := range gptmq.ctx.Fields {
		if !grouprofiletypemapping.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("genclients: invalid field %q for query", f)}
		}
	}
	if gptmq.path != nil {
		prev, err := gptmq.path(ctx)
		if err != nil {
			return err
		}
		gptmq.sql = prev
	}
	return nil
}

func (gptmq *GrouProfileTypeMappingQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*GrouProfileTypeMapping, error) {
	var (
		nodes = []*GrouProfileTypeMapping{}
		_spec = gptmq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*GrouProfileTypeMapping).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &GrouProfileTypeMapping{config: gptmq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, gptmq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (gptmq *GrouProfileTypeMappingQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := gptmq.querySpec()
	_spec.Node.Columns = gptmq.ctx.Fields
	if len(gptmq.ctx.Fields) > 0 {
		_spec.Unique = gptmq.ctx.Unique != nil && *gptmq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, gptmq.driver, _spec)
}

func (gptmq *GrouProfileTypeMappingQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(grouprofiletypemapping.Table, grouprofiletypemapping.Columns, sqlgraph.NewFieldSpec(grouprofiletypemapping.FieldID, field.TypeInt64))
	_spec.From = gptmq.sql
	if unique := gptmq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if gptmq.path != nil {
		_spec.Unique = true
	}
	if fields := gptmq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, grouprofiletypemapping.FieldID)
		for i := range fields {
			if fields[i] != grouprofiletypemapping.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := gptmq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := gptmq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := gptmq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := gptmq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (gptmq *GrouProfileTypeMappingQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(gptmq.driver.Dialect())
	t1 := builder.Table(grouprofiletypemapping.Table)
	columns := gptmq.ctx.Fields
	if len(columns) == 0 {
		columns = grouprofiletypemapping.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if gptmq.sql != nil {
		selector = gptmq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if gptmq.ctx.Unique != nil && *gptmq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range gptmq.predicates {
		p(selector)
	}
	for _, p := range gptmq.order {
		p(selector)
	}
	if offset := gptmq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := gptmq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// GrouProfileTypeMappingGroupBy is the group-by builder for GrouProfileTypeMapping entities.
type GrouProfileTypeMappingGroupBy struct {
	selector
	build *GrouProfileTypeMappingQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (gptmgb *GrouProfileTypeMappingGroupBy) Aggregate(fns ...AggregateFunc) *GrouProfileTypeMappingGroupBy {
	gptmgb.fns = append(gptmgb.fns, fns...)
	return gptmgb
}

// Scan applies the selector query and scans the result into the given value.
func (gptmgb *GrouProfileTypeMappingGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, gptmgb.build.ctx, ent.OpQueryGroupBy)
	if err := gptmgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*GrouProfileTypeMappingQuery, *GrouProfileTypeMappingGroupBy](ctx, gptmgb.build, gptmgb, gptmgb.build.inters, v)
}

func (gptmgb *GrouProfileTypeMappingGroupBy) sqlScan(ctx context.Context, root *GrouProfileTypeMappingQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(gptmgb.fns))
	for _, fn := range gptmgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*gptmgb.flds)+len(gptmgb.fns))
		for _, f := range *gptmgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*gptmgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := gptmgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// GrouProfileTypeMappingSelect is the builder for selecting fields of GrouProfileTypeMapping entities.
type GrouProfileTypeMappingSelect struct {
	*GrouProfileTypeMappingQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (gptms *GrouProfileTypeMappingSelect) Aggregate(fns ...AggregateFunc) *GrouProfileTypeMappingSelect {
	gptms.fns = append(gptms.fns, fns...)
	return gptms
}

// Scan applies the selector query and scans the result into the given value.
func (gptms *GrouProfileTypeMappingSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, gptms.ctx, ent.OpQuerySelect)
	if err := gptms.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*GrouProfileTypeMappingQuery, *GrouProfileTypeMappingSelect](ctx, gptms.GrouProfileTypeMappingQuery, gptms, gptms.inters, v)
}

func (gptms *GrouProfileTypeMappingSelect) sqlScan(ctx context.Context, root *GrouProfileTypeMappingQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(gptms.fns))
	for _, fn := range gptms.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*gptms.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := gptms.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
