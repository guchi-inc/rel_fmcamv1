// Code generated by ent, DO NOT EDIT.

package genclients

import (
	"context"
	"fmcam/systems/genclients/fmdedicatedservices"
	"fmcam/systems/genclients/predicate"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// FmDedicatedServicesQuery is the builder for querying FmDedicatedServices entities.
type FmDedicatedServicesQuery struct {
	config
	ctx        *QueryContext
	order      []fmdedicatedservices.OrderOption
	inters     []Interceptor
	predicates []predicate.FmDedicatedServices
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the FmDedicatedServicesQuery builder.
func (fdsq *FmDedicatedServicesQuery) Where(ps ...predicate.FmDedicatedServices) *FmDedicatedServicesQuery {
	fdsq.predicates = append(fdsq.predicates, ps...)
	return fdsq
}

// Limit the number of records to be returned by this query.
func (fdsq *FmDedicatedServicesQuery) Limit(limit int) *FmDedicatedServicesQuery {
	fdsq.ctx.Limit = &limit
	return fdsq
}

// Offset to start from.
func (fdsq *FmDedicatedServicesQuery) Offset(offset int) *FmDedicatedServicesQuery {
	fdsq.ctx.Offset = &offset
	return fdsq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (fdsq *FmDedicatedServicesQuery) Unique(unique bool) *FmDedicatedServicesQuery {
	fdsq.ctx.Unique = &unique
	return fdsq
}

// Order specifies how the records should be ordered.
func (fdsq *FmDedicatedServicesQuery) Order(o ...fmdedicatedservices.OrderOption) *FmDedicatedServicesQuery {
	fdsq.order = append(fdsq.order, o...)
	return fdsq
}

// First returns the first FmDedicatedServices entity from the query.
// Returns a *NotFoundError when no FmDedicatedServices was found.
func (fdsq *FmDedicatedServicesQuery) First(ctx context.Context) (*FmDedicatedServices, error) {
	nodes, err := fdsq.Limit(1).All(setContextOp(ctx, fdsq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{fmdedicatedservices.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (fdsq *FmDedicatedServicesQuery) FirstX(ctx context.Context) *FmDedicatedServices {
	node, err := fdsq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first FmDedicatedServices ID from the query.
// Returns a *NotFoundError when no FmDedicatedServices ID was found.
func (fdsq *FmDedicatedServicesQuery) FirstID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = fdsq.Limit(1).IDs(setContextOp(ctx, fdsq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{fmdedicatedservices.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (fdsq *FmDedicatedServicesQuery) FirstIDX(ctx context.Context) int64 {
	id, err := fdsq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single FmDedicatedServices entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one FmDedicatedServices entity is found.
// Returns a *NotFoundError when no FmDedicatedServices entities are found.
func (fdsq *FmDedicatedServicesQuery) Only(ctx context.Context) (*FmDedicatedServices, error) {
	nodes, err := fdsq.Limit(2).All(setContextOp(ctx, fdsq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{fmdedicatedservices.Label}
	default:
		return nil, &NotSingularError{fmdedicatedservices.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (fdsq *FmDedicatedServicesQuery) OnlyX(ctx context.Context) *FmDedicatedServices {
	node, err := fdsq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only FmDedicatedServices ID in the query.
// Returns a *NotSingularError when more than one FmDedicatedServices ID is found.
// Returns a *NotFoundError when no entities are found.
func (fdsq *FmDedicatedServicesQuery) OnlyID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = fdsq.Limit(2).IDs(setContextOp(ctx, fdsq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{fmdedicatedservices.Label}
	default:
		err = &NotSingularError{fmdedicatedservices.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (fdsq *FmDedicatedServicesQuery) OnlyIDX(ctx context.Context) int64 {
	id, err := fdsq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of FmDedicatedServicesSlice.
func (fdsq *FmDedicatedServicesQuery) All(ctx context.Context) ([]*FmDedicatedServices, error) {
	ctx = setContextOp(ctx, fdsq.ctx, ent.OpQueryAll)
	if err := fdsq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*FmDedicatedServices, *FmDedicatedServicesQuery]()
	return withInterceptors[[]*FmDedicatedServices](ctx, fdsq, qr, fdsq.inters)
}

// AllX is like All, but panics if an error occurs.
func (fdsq *FmDedicatedServicesQuery) AllX(ctx context.Context) []*FmDedicatedServices {
	nodes, err := fdsq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of FmDedicatedServices IDs.
func (fdsq *FmDedicatedServicesQuery) IDs(ctx context.Context) (ids []int64, err error) {
	if fdsq.ctx.Unique == nil && fdsq.path != nil {
		fdsq.Unique(true)
	}
	ctx = setContextOp(ctx, fdsq.ctx, ent.OpQueryIDs)
	if err = fdsq.Select(fmdedicatedservices.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (fdsq *FmDedicatedServicesQuery) IDsX(ctx context.Context) []int64 {
	ids, err := fdsq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (fdsq *FmDedicatedServicesQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, fdsq.ctx, ent.OpQueryCount)
	if err := fdsq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, fdsq, querierCount[*FmDedicatedServicesQuery](), fdsq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (fdsq *FmDedicatedServicesQuery) CountX(ctx context.Context) int {
	count, err := fdsq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (fdsq *FmDedicatedServicesQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, fdsq.ctx, ent.OpQueryExist)
	switch _, err := fdsq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("genclients: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (fdsq *FmDedicatedServicesQuery) ExistX(ctx context.Context) bool {
	exist, err := fdsq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the FmDedicatedServicesQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (fdsq *FmDedicatedServicesQuery) Clone() *FmDedicatedServicesQuery {
	if fdsq == nil {
		return nil
	}
	return &FmDedicatedServicesQuery{
		config:     fdsq.config,
		ctx:        fdsq.ctx.Clone(),
		order:      append([]fmdedicatedservices.OrderOption{}, fdsq.order...),
		inters:     append([]Interceptor{}, fdsq.inters...),
		predicates: append([]predicate.FmDedicatedServices{}, fdsq.predicates...),
		// clone intermediate query.
		sql:  fdsq.sql.Clone(),
		path: fdsq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		WorkID int64 `json:"work_id" db:"work_id"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.FmDedicatedServices.Query().
//		GroupBy(fmdedicatedservices.FieldWorkID).
//		Aggregate(genclients.Count()).
//		Scan(ctx, &v)
func (fdsq *FmDedicatedServicesQuery) GroupBy(field string, fields ...string) *FmDedicatedServicesGroupBy {
	fdsq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &FmDedicatedServicesGroupBy{build: fdsq}
	grbuild.flds = &fdsq.ctx.Fields
	grbuild.label = fmdedicatedservices.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		WorkID int64 `json:"work_id" db:"work_id"`
//	}
//
//	client.FmDedicatedServices.Query().
//		Select(fmdedicatedservices.FieldWorkID).
//		Scan(ctx, &v)
func (fdsq *FmDedicatedServicesQuery) Select(fields ...string) *FmDedicatedServicesSelect {
	fdsq.ctx.Fields = append(fdsq.ctx.Fields, fields...)
	sbuild := &FmDedicatedServicesSelect{FmDedicatedServicesQuery: fdsq}
	sbuild.label = fmdedicatedservices.Label
	sbuild.flds, sbuild.scan = &fdsq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a FmDedicatedServicesSelect configured with the given aggregations.
func (fdsq *FmDedicatedServicesQuery) Aggregate(fns ...AggregateFunc) *FmDedicatedServicesSelect {
	return fdsq.Select().Aggregate(fns...)
}

func (fdsq *FmDedicatedServicesQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range fdsq.inters {
		if inter == nil {
			return fmt.Errorf("genclients: uninitialized interceptor (forgotten import genclients/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, fdsq); err != nil {
				return err
			}
		}
	}
	for _, f := range fdsq.ctx.Fields {
		if !fmdedicatedservices.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("genclients: invalid field %q for query", f)}
		}
	}
	if fdsq.path != nil {
		prev, err := fdsq.path(ctx)
		if err != nil {
			return err
		}
		fdsq.sql = prev
	}
	return nil
}

func (fdsq *FmDedicatedServicesQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*FmDedicatedServices, error) {
	var (
		nodes = []*FmDedicatedServices{}
		_spec = fdsq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*FmDedicatedServices).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &FmDedicatedServices{config: fdsq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, fdsq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (fdsq *FmDedicatedServicesQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := fdsq.querySpec()
	_spec.Node.Columns = fdsq.ctx.Fields
	if len(fdsq.ctx.Fields) > 0 {
		_spec.Unique = fdsq.ctx.Unique != nil && *fdsq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, fdsq.driver, _spec)
}

func (fdsq *FmDedicatedServicesQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(fmdedicatedservices.Table, fmdedicatedservices.Columns, sqlgraph.NewFieldSpec(fmdedicatedservices.FieldID, field.TypeInt64))
	_spec.From = fdsq.sql
	if unique := fdsq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if fdsq.path != nil {
		_spec.Unique = true
	}
	if fields := fdsq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, fmdedicatedservices.FieldID)
		for i := range fields {
			if fields[i] != fmdedicatedservices.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := fdsq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := fdsq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := fdsq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := fdsq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (fdsq *FmDedicatedServicesQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(fdsq.driver.Dialect())
	t1 := builder.Table(fmdedicatedservices.Table)
	columns := fdsq.ctx.Fields
	if len(columns) == 0 {
		columns = fmdedicatedservices.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if fdsq.sql != nil {
		selector = fdsq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if fdsq.ctx.Unique != nil && *fdsq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range fdsq.predicates {
		p(selector)
	}
	for _, p := range fdsq.order {
		p(selector)
	}
	if offset := fdsq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := fdsq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// FmDedicatedServicesGroupBy is the group-by builder for FmDedicatedServices entities.
type FmDedicatedServicesGroupBy struct {
	selector
	build *FmDedicatedServicesQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (fdsgb *FmDedicatedServicesGroupBy) Aggregate(fns ...AggregateFunc) *FmDedicatedServicesGroupBy {
	fdsgb.fns = append(fdsgb.fns, fns...)
	return fdsgb
}

// Scan applies the selector query and scans the result into the given value.
func (fdsgb *FmDedicatedServicesGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, fdsgb.build.ctx, ent.OpQueryGroupBy)
	if err := fdsgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*FmDedicatedServicesQuery, *FmDedicatedServicesGroupBy](ctx, fdsgb.build, fdsgb, fdsgb.build.inters, v)
}

func (fdsgb *FmDedicatedServicesGroupBy) sqlScan(ctx context.Context, root *FmDedicatedServicesQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(fdsgb.fns))
	for _, fn := range fdsgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*fdsgb.flds)+len(fdsgb.fns))
		for _, f := range *fdsgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*fdsgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := fdsgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// FmDedicatedServicesSelect is the builder for selecting fields of FmDedicatedServices entities.
type FmDedicatedServicesSelect struct {
	*FmDedicatedServicesQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (fdss *FmDedicatedServicesSelect) Aggregate(fns ...AggregateFunc) *FmDedicatedServicesSelect {
	fdss.fns = append(fdss.fns, fns...)
	return fdss
}

// Scan applies the selector query and scans the result into the given value.
func (fdss *FmDedicatedServicesSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, fdss.ctx, ent.OpQuerySelect)
	if err := fdss.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*FmDedicatedServicesQuery, *FmDedicatedServicesSelect](ctx, fdss.FmDedicatedServicesQuery, fdss, fdss.inters, v)
}

func (fdss *FmDedicatedServicesSelect) sqlScan(ctx context.Context, root *FmDedicatedServicesQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(fdss.fns))
	for _, fn := range fdss.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*fdss.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := fdss.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
