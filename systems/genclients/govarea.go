// Code generated by ent, DO NOT EDIT.

package genclients

import (
	"fmcam/systems/genclients/govarea"
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// GovArea is the model entity for the GovArea schema.
type GovArea struct {
	config `db:"-" json:"-"`
	// ID of the ent.
	ID int64 `json:"id" db:"id"`
	// 编号
	Code string `json:"code"  db:"code"`
	// 名称
	Name string `json:"name"  db:"name"`
	// 省编号
	ProvinceCode string `json:"province_code"  db:"province_code"`
	// 市编号
	CityCode string `json:"city_code"  db:"city_code"`
	// 操作人
	Creator string `json:"creator"  db:"creator"`
	// 删除标记
	DeleteFlag string `json:"delete_flag"  db:"delete_flag"`
	// 创建时间
	CreatedTime  *time.Time `json:"created_time" db:"created_time"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*GovArea) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case govarea.FieldID:
			values[i] = new(sql.NullInt64)
		case govarea.FieldCode, govarea.FieldName, govarea.FieldProvinceCode, govarea.FieldCityCode, govarea.FieldCreator, govarea.FieldDeleteFlag:
			values[i] = new(sql.NullString)
		case govarea.FieldCreatedTime:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the GovArea fields.
func (ga *GovArea) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case govarea.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			ga.ID = int64(value.Int64)
		case govarea.FieldCode:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field code", values[i])
			} else if value.Valid {
				ga.Code = value.String
			}
		case govarea.FieldName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field name", values[i])
			} else if value.Valid {
				ga.Name = value.String
			}
		case govarea.FieldProvinceCode:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field province_code", values[i])
			} else if value.Valid {
				ga.ProvinceCode = value.String
			}
		case govarea.FieldCityCode:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field city_code", values[i])
			} else if value.Valid {
				ga.CityCode = value.String
			}
		case govarea.FieldCreator:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field creator", values[i])
			} else if value.Valid {
				ga.Creator = value.String
			}
		case govarea.FieldDeleteFlag:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field delete_flag", values[i])
			} else if value.Valid {
				ga.DeleteFlag = value.String
			}
		case govarea.FieldCreatedTime:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_time", values[i])
			} else if value.Valid {
				ga.CreatedTime = new(time.Time)
				*ga.CreatedTime = value.Time
			}
		default:
			ga.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the GovArea.
// This includes values selected through modifiers, order, etc.
func (ga *GovArea) Value(name string) (ent.Value, error) {
	return ga.selectValues.Get(name)
}

// Update returns a builder for updating this GovArea.
// Note that you need to call GovArea.Unwrap() before calling this method if this GovArea
// was returned from a transaction, and the transaction was committed or rolled back.
func (ga *GovArea) Update() *GovAreaUpdateOne {
	return NewGovAreaClient(ga.config).UpdateOne(ga)
}

// Unwrap unwraps the GovArea entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (ga *GovArea) Unwrap() *GovArea {
	_tx, ok := ga.config.driver.(*txDriver)
	if !ok {
		panic("genclients: GovArea is not a transactional entity")
	}
	ga.config.driver = _tx.drv
	return ga
}

// String implements the fmt.Stringer.
func (ga *GovArea) String() string {
	var builder strings.Builder
	builder.WriteString("GovArea(")
	builder.WriteString(fmt.Sprintf("id=%v, ", ga.ID))
	builder.WriteString("code=")
	builder.WriteString(ga.Code)
	builder.WriteString(", ")
	builder.WriteString("name=")
	builder.WriteString(ga.Name)
	builder.WriteString(", ")
	builder.WriteString("province_code=")
	builder.WriteString(ga.ProvinceCode)
	builder.WriteString(", ")
	builder.WriteString("city_code=")
	builder.WriteString(ga.CityCode)
	builder.WriteString(", ")
	builder.WriteString("creator=")
	builder.WriteString(ga.Creator)
	builder.WriteString(", ")
	builder.WriteString("delete_flag=")
	builder.WriteString(ga.DeleteFlag)
	builder.WriteString(", ")
	if v := ga.CreatedTime; v != nil {
		builder.WriteString("created_time=")
		builder.WriteString(v.Format(time.ANSIC))
	}
	builder.WriteByte(')')
	return builder.String()
}

// GovAreas is a parsable slice of GovArea.
type GovAreas []*GovArea
