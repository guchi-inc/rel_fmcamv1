// Code generated by ent, DO NOT EDIT.

package genclients

import (
	"context"
	"fmcam/systems/genclients/fmuseraccount"
	"fmcam/systems/genclients/predicate"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// FmUserAccountQuery is the builder for querying FmUserAccount entities.
type FmUserAccountQuery struct {
	config
	ctx        *QueryContext
	order      []fmuseraccount.OrderOption
	inters     []Interceptor
	predicates []predicate.FmUserAccount
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the FmUserAccountQuery builder.
func (fuaq *FmUserAccountQuery) Where(ps ...predicate.FmUserAccount) *FmUserAccountQuery {
	fuaq.predicates = append(fuaq.predicates, ps...)
	return fuaq
}

// Limit the number of records to be returned by this query.
func (fuaq *FmUserAccountQuery) Limit(limit int) *FmUserAccountQuery {
	fuaq.ctx.Limit = &limit
	return fuaq
}

// Offset to start from.
func (fuaq *FmUserAccountQuery) Offset(offset int) *FmUserAccountQuery {
	fuaq.ctx.Offset = &offset
	return fuaq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (fuaq *FmUserAccountQuery) Unique(unique bool) *FmUserAccountQuery {
	fuaq.ctx.Unique = &unique
	return fuaq
}

// Order specifies how the records should be ordered.
func (fuaq *FmUserAccountQuery) Order(o ...fmuseraccount.OrderOption) *FmUserAccountQuery {
	fuaq.order = append(fuaq.order, o...)
	return fuaq
}

// First returns the first FmUserAccount entity from the query.
// Returns a *NotFoundError when no FmUserAccount was found.
func (fuaq *FmUserAccountQuery) First(ctx context.Context) (*FmUserAccount, error) {
	nodes, err := fuaq.Limit(1).All(setContextOp(ctx, fuaq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{fmuseraccount.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (fuaq *FmUserAccountQuery) FirstX(ctx context.Context) *FmUserAccount {
	node, err := fuaq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first FmUserAccount ID from the query.
// Returns a *NotFoundError when no FmUserAccount ID was found.
func (fuaq *FmUserAccountQuery) FirstID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = fuaq.Limit(1).IDs(setContextOp(ctx, fuaq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{fmuseraccount.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (fuaq *FmUserAccountQuery) FirstIDX(ctx context.Context) int64 {
	id, err := fuaq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single FmUserAccount entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one FmUserAccount entity is found.
// Returns a *NotFoundError when no FmUserAccount entities are found.
func (fuaq *FmUserAccountQuery) Only(ctx context.Context) (*FmUserAccount, error) {
	nodes, err := fuaq.Limit(2).All(setContextOp(ctx, fuaq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{fmuseraccount.Label}
	default:
		return nil, &NotSingularError{fmuseraccount.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (fuaq *FmUserAccountQuery) OnlyX(ctx context.Context) *FmUserAccount {
	node, err := fuaq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only FmUserAccount ID in the query.
// Returns a *NotSingularError when more than one FmUserAccount ID is found.
// Returns a *NotFoundError when no entities are found.
func (fuaq *FmUserAccountQuery) OnlyID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = fuaq.Limit(2).IDs(setContextOp(ctx, fuaq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{fmuseraccount.Label}
	default:
		err = &NotSingularError{fmuseraccount.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (fuaq *FmUserAccountQuery) OnlyIDX(ctx context.Context) int64 {
	id, err := fuaq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of FmUserAccounts.
func (fuaq *FmUserAccountQuery) All(ctx context.Context) ([]*FmUserAccount, error) {
	ctx = setContextOp(ctx, fuaq.ctx, ent.OpQueryAll)
	if err := fuaq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*FmUserAccount, *FmUserAccountQuery]()
	return withInterceptors[[]*FmUserAccount](ctx, fuaq, qr, fuaq.inters)
}

// AllX is like All, but panics if an error occurs.
func (fuaq *FmUserAccountQuery) AllX(ctx context.Context) []*FmUserAccount {
	nodes, err := fuaq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of FmUserAccount IDs.
func (fuaq *FmUserAccountQuery) IDs(ctx context.Context) (ids []int64, err error) {
	if fuaq.ctx.Unique == nil && fuaq.path != nil {
		fuaq.Unique(true)
	}
	ctx = setContextOp(ctx, fuaq.ctx, ent.OpQueryIDs)
	if err = fuaq.Select(fmuseraccount.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (fuaq *FmUserAccountQuery) IDsX(ctx context.Context) []int64 {
	ids, err := fuaq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (fuaq *FmUserAccountQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, fuaq.ctx, ent.OpQueryCount)
	if err := fuaq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, fuaq, querierCount[*FmUserAccountQuery](), fuaq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (fuaq *FmUserAccountQuery) CountX(ctx context.Context) int {
	count, err := fuaq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (fuaq *FmUserAccountQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, fuaq.ctx, ent.OpQueryExist)
	switch _, err := fuaq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("genclients: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (fuaq *FmUserAccountQuery) ExistX(ctx context.Context) bool {
	exist, err := fuaq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the FmUserAccountQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (fuaq *FmUserAccountQuery) Clone() *FmUserAccountQuery {
	if fuaq == nil {
		return nil
	}
	return &FmUserAccountQuery{
		config:     fuaq.config,
		ctx:        fuaq.ctx.Clone(),
		order:      append([]fmuseraccount.OrderOption{}, fuaq.order...),
		inters:     append([]Interceptor{}, fuaq.inters...),
		predicates: append([]predicate.FmUserAccount{}, fuaq.predicates...),
		// clone intermediate query.
		sql:  fuaq.sql.Clone(),
		path: fuaq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Username string `json:"username"  db:"username"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.FmUserAccount.Query().
//		GroupBy(fmuseraccount.FieldUsername).
//		Aggregate(genclients.Count()).
//		Scan(ctx, &v)
func (fuaq *FmUserAccountQuery) GroupBy(field string, fields ...string) *FmUserAccountGroupBy {
	fuaq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &FmUserAccountGroupBy{build: fuaq}
	grbuild.flds = &fuaq.ctx.Fields
	grbuild.label = fmuseraccount.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Username string `json:"username"  db:"username"`
//	}
//
//	client.FmUserAccount.Query().
//		Select(fmuseraccount.FieldUsername).
//		Scan(ctx, &v)
func (fuaq *FmUserAccountQuery) Select(fields ...string) *FmUserAccountSelect {
	fuaq.ctx.Fields = append(fuaq.ctx.Fields, fields...)
	sbuild := &FmUserAccountSelect{FmUserAccountQuery: fuaq}
	sbuild.label = fmuseraccount.Label
	sbuild.flds, sbuild.scan = &fuaq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a FmUserAccountSelect configured with the given aggregations.
func (fuaq *FmUserAccountQuery) Aggregate(fns ...AggregateFunc) *FmUserAccountSelect {
	return fuaq.Select().Aggregate(fns...)
}

func (fuaq *FmUserAccountQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range fuaq.inters {
		if inter == nil {
			return fmt.Errorf("genclients: uninitialized interceptor (forgotten import genclients/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, fuaq); err != nil {
				return err
			}
		}
	}
	for _, f := range fuaq.ctx.Fields {
		if !fmuseraccount.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("genclients: invalid field %q for query", f)}
		}
	}
	if fuaq.path != nil {
		prev, err := fuaq.path(ctx)
		if err != nil {
			return err
		}
		fuaq.sql = prev
	}
	return nil
}

func (fuaq *FmUserAccountQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*FmUserAccount, error) {
	var (
		nodes = []*FmUserAccount{}
		_spec = fuaq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*FmUserAccount).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &FmUserAccount{config: fuaq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, fuaq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (fuaq *FmUserAccountQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := fuaq.querySpec()
	_spec.Node.Columns = fuaq.ctx.Fields
	if len(fuaq.ctx.Fields) > 0 {
		_spec.Unique = fuaq.ctx.Unique != nil && *fuaq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, fuaq.driver, _spec)
}

func (fuaq *FmUserAccountQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(fmuseraccount.Table, fmuseraccount.Columns, sqlgraph.NewFieldSpec(fmuseraccount.FieldID, field.TypeInt64))
	_spec.From = fuaq.sql
	if unique := fuaq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if fuaq.path != nil {
		_spec.Unique = true
	}
	if fields := fuaq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, fmuseraccount.FieldID)
		for i := range fields {
			if fields[i] != fmuseraccount.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := fuaq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := fuaq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := fuaq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := fuaq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (fuaq *FmUserAccountQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(fuaq.driver.Dialect())
	t1 := builder.Table(fmuseraccount.Table)
	columns := fuaq.ctx.Fields
	if len(columns) == 0 {
		columns = fmuseraccount.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if fuaq.sql != nil {
		selector = fuaq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if fuaq.ctx.Unique != nil && *fuaq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range fuaq.predicates {
		p(selector)
	}
	for _, p := range fuaq.order {
		p(selector)
	}
	if offset := fuaq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := fuaq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// FmUserAccountGroupBy is the group-by builder for FmUserAccount entities.
type FmUserAccountGroupBy struct {
	selector
	build *FmUserAccountQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (fuagb *FmUserAccountGroupBy) Aggregate(fns ...AggregateFunc) *FmUserAccountGroupBy {
	fuagb.fns = append(fuagb.fns, fns...)
	return fuagb
}

// Scan applies the selector query and scans the result into the given value.
func (fuagb *FmUserAccountGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, fuagb.build.ctx, ent.OpQueryGroupBy)
	if err := fuagb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*FmUserAccountQuery, *FmUserAccountGroupBy](ctx, fuagb.build, fuagb, fuagb.build.inters, v)
}

func (fuagb *FmUserAccountGroupBy) sqlScan(ctx context.Context, root *FmUserAccountQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(fuagb.fns))
	for _, fn := range fuagb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*fuagb.flds)+len(fuagb.fns))
		for _, f := range *fuagb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*fuagb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := fuagb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// FmUserAccountSelect is the builder for selecting fields of FmUserAccount entities.
type FmUserAccountSelect struct {
	*FmUserAccountQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (fuas *FmUserAccountSelect) Aggregate(fns ...AggregateFunc) *FmUserAccountSelect {
	fuas.fns = append(fuas.fns, fns...)
	return fuas
}

// Scan applies the selector query and scans the result into the given value.
func (fuas *FmUserAccountSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, fuas.ctx, ent.OpQuerySelect)
	if err := fuas.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*FmUserAccountQuery, *FmUserAccountSelect](ctx, fuas.FmUserAccountQuery, fuas, fuas.inters, v)
}

func (fuas *FmUserAccountSelect) sqlScan(ctx context.Context, root *FmUserAccountQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(fuas.fns))
	for _, fn := range fuas.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*fuas.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := fuas.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
