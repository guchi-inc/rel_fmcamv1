// Code generated by ent, DO NOT EDIT.

package genclients

import (
	"context"
	"fmcam/systems/genclients/govarea"
	"fmcam/systems/genclients/predicate"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// GovAreaQuery is the builder for querying GovArea entities.
type GovAreaQuery struct {
	config
	ctx        *QueryContext
	order      []govarea.OrderOption
	inters     []Interceptor
	predicates []predicate.GovArea
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the GovAreaQuery builder.
func (gaq *GovAreaQuery) Where(ps ...predicate.GovArea) *GovAreaQuery {
	gaq.predicates = append(gaq.predicates, ps...)
	return gaq
}

// Limit the number of records to be returned by this query.
func (gaq *GovAreaQuery) Limit(limit int) *GovAreaQuery {
	gaq.ctx.Limit = &limit
	return gaq
}

// Offset to start from.
func (gaq *GovAreaQuery) Offset(offset int) *GovAreaQuery {
	gaq.ctx.Offset = &offset
	return gaq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (gaq *GovAreaQuery) Unique(unique bool) *GovAreaQuery {
	gaq.ctx.Unique = &unique
	return gaq
}

// Order specifies how the records should be ordered.
func (gaq *GovAreaQuery) Order(o ...govarea.OrderOption) *GovAreaQuery {
	gaq.order = append(gaq.order, o...)
	return gaq
}

// First returns the first GovArea entity from the query.
// Returns a *NotFoundError when no GovArea was found.
func (gaq *GovAreaQuery) First(ctx context.Context) (*GovArea, error) {
	nodes, err := gaq.Limit(1).All(setContextOp(ctx, gaq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{govarea.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (gaq *GovAreaQuery) FirstX(ctx context.Context) *GovArea {
	node, err := gaq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first GovArea ID from the query.
// Returns a *NotFoundError when no GovArea ID was found.
func (gaq *GovAreaQuery) FirstID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = gaq.Limit(1).IDs(setContextOp(ctx, gaq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{govarea.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (gaq *GovAreaQuery) FirstIDX(ctx context.Context) int64 {
	id, err := gaq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single GovArea entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one GovArea entity is found.
// Returns a *NotFoundError when no GovArea entities are found.
func (gaq *GovAreaQuery) Only(ctx context.Context) (*GovArea, error) {
	nodes, err := gaq.Limit(2).All(setContextOp(ctx, gaq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{govarea.Label}
	default:
		return nil, &NotSingularError{govarea.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (gaq *GovAreaQuery) OnlyX(ctx context.Context) *GovArea {
	node, err := gaq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only GovArea ID in the query.
// Returns a *NotSingularError when more than one GovArea ID is found.
// Returns a *NotFoundError when no entities are found.
func (gaq *GovAreaQuery) OnlyID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = gaq.Limit(2).IDs(setContextOp(ctx, gaq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{govarea.Label}
	default:
		err = &NotSingularError{govarea.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (gaq *GovAreaQuery) OnlyIDX(ctx context.Context) int64 {
	id, err := gaq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of GovAreas.
func (gaq *GovAreaQuery) All(ctx context.Context) ([]*GovArea, error) {
	ctx = setContextOp(ctx, gaq.ctx, ent.OpQueryAll)
	if err := gaq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*GovArea, *GovAreaQuery]()
	return withInterceptors[[]*GovArea](ctx, gaq, qr, gaq.inters)
}

// AllX is like All, but panics if an error occurs.
func (gaq *GovAreaQuery) AllX(ctx context.Context) []*GovArea {
	nodes, err := gaq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of GovArea IDs.
func (gaq *GovAreaQuery) IDs(ctx context.Context) (ids []int64, err error) {
	if gaq.ctx.Unique == nil && gaq.path != nil {
		gaq.Unique(true)
	}
	ctx = setContextOp(ctx, gaq.ctx, ent.OpQueryIDs)
	if err = gaq.Select(govarea.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (gaq *GovAreaQuery) IDsX(ctx context.Context) []int64 {
	ids, err := gaq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (gaq *GovAreaQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, gaq.ctx, ent.OpQueryCount)
	if err := gaq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, gaq, querierCount[*GovAreaQuery](), gaq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (gaq *GovAreaQuery) CountX(ctx context.Context) int {
	count, err := gaq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (gaq *GovAreaQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, gaq.ctx, ent.OpQueryExist)
	switch _, err := gaq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("genclients: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (gaq *GovAreaQuery) ExistX(ctx context.Context) bool {
	exist, err := gaq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the GovAreaQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (gaq *GovAreaQuery) Clone() *GovAreaQuery {
	if gaq == nil {
		return nil
	}
	return &GovAreaQuery{
		config:     gaq.config,
		ctx:        gaq.ctx.Clone(),
		order:      append([]govarea.OrderOption{}, gaq.order...),
		inters:     append([]Interceptor{}, gaq.inters...),
		predicates: append([]predicate.GovArea{}, gaq.predicates...),
		// clone intermediate query.
		sql:  gaq.sql.Clone(),
		path: gaq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Code string `json:"code"  db:"code"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.GovArea.Query().
//		GroupBy(govarea.FieldCode).
//		Aggregate(genclients.Count()).
//		Scan(ctx, &v)
func (gaq *GovAreaQuery) GroupBy(field string, fields ...string) *GovAreaGroupBy {
	gaq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &GovAreaGroupBy{build: gaq}
	grbuild.flds = &gaq.ctx.Fields
	grbuild.label = govarea.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Code string `json:"code"  db:"code"`
//	}
//
//	client.GovArea.Query().
//		Select(govarea.FieldCode).
//		Scan(ctx, &v)
func (gaq *GovAreaQuery) Select(fields ...string) *GovAreaSelect {
	gaq.ctx.Fields = append(gaq.ctx.Fields, fields...)
	sbuild := &GovAreaSelect{GovAreaQuery: gaq}
	sbuild.label = govarea.Label
	sbuild.flds, sbuild.scan = &gaq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a GovAreaSelect configured with the given aggregations.
func (gaq *GovAreaQuery) Aggregate(fns ...AggregateFunc) *GovAreaSelect {
	return gaq.Select().Aggregate(fns...)
}

func (gaq *GovAreaQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range gaq.inters {
		if inter == nil {
			return fmt.Errorf("genclients: uninitialized interceptor (forgotten import genclients/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, gaq); err != nil {
				return err
			}
		}
	}
	for _, f := range gaq.ctx.Fields {
		if !govarea.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("genclients: invalid field %q for query", f)}
		}
	}
	if gaq.path != nil {
		prev, err := gaq.path(ctx)
		if err != nil {
			return err
		}
		gaq.sql = prev
	}
	return nil
}

func (gaq *GovAreaQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*GovArea, error) {
	var (
		nodes = []*GovArea{}
		_spec = gaq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*GovArea).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &GovArea{config: gaq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, gaq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (gaq *GovAreaQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := gaq.querySpec()
	_spec.Node.Columns = gaq.ctx.Fields
	if len(gaq.ctx.Fields) > 0 {
		_spec.Unique = gaq.ctx.Unique != nil && *gaq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, gaq.driver, _spec)
}

func (gaq *GovAreaQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(govarea.Table, govarea.Columns, sqlgraph.NewFieldSpec(govarea.FieldID, field.TypeInt64))
	_spec.From = gaq.sql
	if unique := gaq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if gaq.path != nil {
		_spec.Unique = true
	}
	if fields := gaq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, govarea.FieldID)
		for i := range fields {
			if fields[i] != govarea.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := gaq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := gaq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := gaq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := gaq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (gaq *GovAreaQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(gaq.driver.Dialect())
	t1 := builder.Table(govarea.Table)
	columns := gaq.ctx.Fields
	if len(columns) == 0 {
		columns = govarea.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if gaq.sql != nil {
		selector = gaq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if gaq.ctx.Unique != nil && *gaq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range gaq.predicates {
		p(selector)
	}
	for _, p := range gaq.order {
		p(selector)
	}
	if offset := gaq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := gaq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// GovAreaGroupBy is the group-by builder for GovArea entities.
type GovAreaGroupBy struct {
	selector
	build *GovAreaQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (gagb *GovAreaGroupBy) Aggregate(fns ...AggregateFunc) *GovAreaGroupBy {
	gagb.fns = append(gagb.fns, fns...)
	return gagb
}

// Scan applies the selector query and scans the result into the given value.
func (gagb *GovAreaGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, gagb.build.ctx, ent.OpQueryGroupBy)
	if err := gagb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*GovAreaQuery, *GovAreaGroupBy](ctx, gagb.build, gagb, gagb.build.inters, v)
}

func (gagb *GovAreaGroupBy) sqlScan(ctx context.Context, root *GovAreaQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(gagb.fns))
	for _, fn := range gagb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*gagb.flds)+len(gagb.fns))
		for _, f := range *gagb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*gagb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := gagb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// GovAreaSelect is the builder for selecting fields of GovArea entities.
type GovAreaSelect struct {
	*GovAreaQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (gas *GovAreaSelect) Aggregate(fns ...AggregateFunc) *GovAreaSelect {
	gas.fns = append(gas.fns, fns...)
	return gas
}

// Scan applies the selector query and scans the result into the given value.
func (gas *GovAreaSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, gas.ctx, ent.OpQuerySelect)
	if err := gas.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*GovAreaQuery, *GovAreaSelect](ctx, gas.GovAreaQuery, gas, gas.inters, v)
}

func (gas *GovAreaSelect) sqlScan(ctx context.Context, root *GovAreaQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(gas.fns))
	for _, fn := range gas.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*gas.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := gas.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
