// Code generated by ent, DO NOT EDIT.

package genclients

import (
	"fmcam/systems/genclients/govstreet"
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// GovStreet is the model entity for the GovStreet schema.
type GovStreet struct {
	config `db:"-" json:"-"`
	// ID of the ent.
	ID int64 `json:"id" db:"id"`
	// 编号
	Code string `json:"code"  db:"code"`
	// 名称
	Name string `json:"name"  db:"name"`
	// 省编号
	ProvinceCode string `json:"province_code"  db:"province_code"`
	// 市编号
	CityCode string `json:"city_code"  db:"city_code"`
	// 区县编号
	AreaCode string `json:"area_code"  db:"area_code"`
	// 操作人
	Creator string `json:"creator"  db:"creator"`
	// 删除标记
	DeleteFlag string `json:"delete_flag"  db:"delete_flag"`
	// 创建时间
	CreatedTime  *time.Time `json:"created_time" db:"created_time"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*GovStreet) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case govstreet.FieldID:
			values[i] = new(sql.NullInt64)
		case govstreet.FieldCode, govstreet.FieldName, govstreet.FieldProvinceCode, govstreet.FieldCityCode, govstreet.FieldAreaCode, govstreet.FieldCreator, govstreet.FieldDeleteFlag:
			values[i] = new(sql.NullString)
		case govstreet.FieldCreatedTime:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the GovStreet fields.
func (gs *GovStreet) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case govstreet.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			gs.ID = int64(value.Int64)
		case govstreet.FieldCode:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field code", values[i])
			} else if value.Valid {
				gs.Code = value.String
			}
		case govstreet.FieldName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field name", values[i])
			} else if value.Valid {
				gs.Name = value.String
			}
		case govstreet.FieldProvinceCode:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field province_code", values[i])
			} else if value.Valid {
				gs.ProvinceCode = value.String
			}
		case govstreet.FieldCityCode:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field city_code", values[i])
			} else if value.Valid {
				gs.CityCode = value.String
			}
		case govstreet.FieldAreaCode:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field area_code", values[i])
			} else if value.Valid {
				gs.AreaCode = value.String
			}
		case govstreet.FieldCreator:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field creator", values[i])
			} else if value.Valid {
				gs.Creator = value.String
			}
		case govstreet.FieldDeleteFlag:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field delete_flag", values[i])
			} else if value.Valid {
				gs.DeleteFlag = value.String
			}
		case govstreet.FieldCreatedTime:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_time", values[i])
			} else if value.Valid {
				gs.CreatedTime = new(time.Time)
				*gs.CreatedTime = value.Time
			}
		default:
			gs.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the GovStreet.
// This includes values selected through modifiers, order, etc.
func (gs *GovStreet) Value(name string) (ent.Value, error) {
	return gs.selectValues.Get(name)
}

// Update returns a builder for updating this GovStreet.
// Note that you need to call GovStreet.Unwrap() before calling this method if this GovStreet
// was returned from a transaction, and the transaction was committed or rolled back.
func (gs *GovStreet) Update() *GovStreetUpdateOne {
	return NewGovStreetClient(gs.config).UpdateOne(gs)
}

// Unwrap unwraps the GovStreet entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (gs *GovStreet) Unwrap() *GovStreet {
	_tx, ok := gs.config.driver.(*txDriver)
	if !ok {
		panic("genclients: GovStreet is not a transactional entity")
	}
	gs.config.driver = _tx.drv
	return gs
}

// String implements the fmt.Stringer.
func (gs *GovStreet) String() string {
	var builder strings.Builder
	builder.WriteString("GovStreet(")
	builder.WriteString(fmt.Sprintf("id=%v, ", gs.ID))
	builder.WriteString("code=")
	builder.WriteString(gs.Code)
	builder.WriteString(", ")
	builder.WriteString("name=")
	builder.WriteString(gs.Name)
	builder.WriteString(", ")
	builder.WriteString("province_code=")
	builder.WriteString(gs.ProvinceCode)
	builder.WriteString(", ")
	builder.WriteString("city_code=")
	builder.WriteString(gs.CityCode)
	builder.WriteString(", ")
	builder.WriteString("area_code=")
	builder.WriteString(gs.AreaCode)
	builder.WriteString(", ")
	builder.WriteString("creator=")
	builder.WriteString(gs.Creator)
	builder.WriteString(", ")
	builder.WriteString("delete_flag=")
	builder.WriteString(gs.DeleteFlag)
	builder.WriteString(", ")
	if v := gs.CreatedTime; v != nil {
		builder.WriteString("created_time=")
		builder.WriteString(v.Format(time.ANSIC))
	}
	builder.WriteByte(')')
	return builder.String()
}

// GovStreets is a parsable slice of GovStreet.
type GovStreets []*GovStreet
