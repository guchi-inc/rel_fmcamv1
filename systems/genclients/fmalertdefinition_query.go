// Code generated by ent, DO NOT EDIT.

package genclients

import (
	"context"
	"fmcam/systems/genclients/fmalertdefinition"
	"fmcam/systems/genclients/predicate"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// FmAlertDefinitionQuery is the builder for querying FmAlertDefinition entities.
type FmAlertDefinitionQuery struct {
	config
	ctx        *QueryContext
	order      []fmalertdefinition.OrderOption
	inters     []Interceptor
	predicates []predicate.FmAlertDefinition
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the FmAlertDefinitionQuery builder.
func (fadq *FmAlertDefinitionQuery) Where(ps ...predicate.FmAlertDefinition) *FmAlertDefinitionQuery {
	fadq.predicates = append(fadq.predicates, ps...)
	return fadq
}

// Limit the number of records to be returned by this query.
func (fadq *FmAlertDefinitionQuery) Limit(limit int) *FmAlertDefinitionQuery {
	fadq.ctx.Limit = &limit
	return fadq
}

// Offset to start from.
func (fadq *FmAlertDefinitionQuery) Offset(offset int) *FmAlertDefinitionQuery {
	fadq.ctx.Offset = &offset
	return fadq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (fadq *FmAlertDefinitionQuery) Unique(unique bool) *FmAlertDefinitionQuery {
	fadq.ctx.Unique = &unique
	return fadq
}

// Order specifies how the records should be ordered.
func (fadq *FmAlertDefinitionQuery) Order(o ...fmalertdefinition.OrderOption) *FmAlertDefinitionQuery {
	fadq.order = append(fadq.order, o...)
	return fadq
}

// First returns the first FmAlertDefinition entity from the query.
// Returns a *NotFoundError when no FmAlertDefinition was found.
func (fadq *FmAlertDefinitionQuery) First(ctx context.Context) (*FmAlertDefinition, error) {
	nodes, err := fadq.Limit(1).All(setContextOp(ctx, fadq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{fmalertdefinition.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (fadq *FmAlertDefinitionQuery) FirstX(ctx context.Context) *FmAlertDefinition {
	node, err := fadq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first FmAlertDefinition ID from the query.
// Returns a *NotFoundError when no FmAlertDefinition ID was found.
func (fadq *FmAlertDefinitionQuery) FirstID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = fadq.Limit(1).IDs(setContextOp(ctx, fadq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{fmalertdefinition.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (fadq *FmAlertDefinitionQuery) FirstIDX(ctx context.Context) int64 {
	id, err := fadq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single FmAlertDefinition entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one FmAlertDefinition entity is found.
// Returns a *NotFoundError when no FmAlertDefinition entities are found.
func (fadq *FmAlertDefinitionQuery) Only(ctx context.Context) (*FmAlertDefinition, error) {
	nodes, err := fadq.Limit(2).All(setContextOp(ctx, fadq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{fmalertdefinition.Label}
	default:
		return nil, &NotSingularError{fmalertdefinition.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (fadq *FmAlertDefinitionQuery) OnlyX(ctx context.Context) *FmAlertDefinition {
	node, err := fadq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only FmAlertDefinition ID in the query.
// Returns a *NotSingularError when more than one FmAlertDefinition ID is found.
// Returns a *NotFoundError when no entities are found.
func (fadq *FmAlertDefinitionQuery) OnlyID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = fadq.Limit(2).IDs(setContextOp(ctx, fadq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{fmalertdefinition.Label}
	default:
		err = &NotSingularError{fmalertdefinition.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (fadq *FmAlertDefinitionQuery) OnlyIDX(ctx context.Context) int64 {
	id, err := fadq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of FmAlertDefinitions.
func (fadq *FmAlertDefinitionQuery) All(ctx context.Context) ([]*FmAlertDefinition, error) {
	ctx = setContextOp(ctx, fadq.ctx, ent.OpQueryAll)
	if err := fadq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*FmAlertDefinition, *FmAlertDefinitionQuery]()
	return withInterceptors[[]*FmAlertDefinition](ctx, fadq, qr, fadq.inters)
}

// AllX is like All, but panics if an error occurs.
func (fadq *FmAlertDefinitionQuery) AllX(ctx context.Context) []*FmAlertDefinition {
	nodes, err := fadq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of FmAlertDefinition IDs.
func (fadq *FmAlertDefinitionQuery) IDs(ctx context.Context) (ids []int64, err error) {
	if fadq.ctx.Unique == nil && fadq.path != nil {
		fadq.Unique(true)
	}
	ctx = setContextOp(ctx, fadq.ctx, ent.OpQueryIDs)
	if err = fadq.Select(fmalertdefinition.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (fadq *FmAlertDefinitionQuery) IDsX(ctx context.Context) []int64 {
	ids, err := fadq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (fadq *FmAlertDefinitionQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, fadq.ctx, ent.OpQueryCount)
	if err := fadq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, fadq, querierCount[*FmAlertDefinitionQuery](), fadq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (fadq *FmAlertDefinitionQuery) CountX(ctx context.Context) int {
	count, err := fadq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (fadq *FmAlertDefinitionQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, fadq.ctx, ent.OpQueryExist)
	switch _, err := fadq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("genclients: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (fadq *FmAlertDefinitionQuery) ExistX(ctx context.Context) bool {
	exist, err := fadq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the FmAlertDefinitionQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (fadq *FmAlertDefinitionQuery) Clone() *FmAlertDefinitionQuery {
	if fadq == nil {
		return nil
	}
	return &FmAlertDefinitionQuery{
		config:     fadq.config,
		ctx:        fadq.ctx.Clone(),
		order:      append([]fmalertdefinition.OrderOption{}, fadq.order...),
		inters:     append([]Interceptor{}, fadq.inters...),
		predicates: append([]predicate.FmAlertDefinition{}, fadq.predicates...),
		// clone intermediate query.
		sql:  fadq.sql.Clone(),
		path: fadq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		AlertGroupID int64 `json:"alert_group_id"  db:"alert_group_id"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.FmAlertDefinition.Query().
//		GroupBy(fmalertdefinition.FieldAlertGroupID).
//		Aggregate(genclients.Count()).
//		Scan(ctx, &v)
func (fadq *FmAlertDefinitionQuery) GroupBy(field string, fields ...string) *FmAlertDefinitionGroupBy {
	fadq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &FmAlertDefinitionGroupBy{build: fadq}
	grbuild.flds = &fadq.ctx.Fields
	grbuild.label = fmalertdefinition.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		AlertGroupID int64 `json:"alert_group_id"  db:"alert_group_id"`
//	}
//
//	client.FmAlertDefinition.Query().
//		Select(fmalertdefinition.FieldAlertGroupID).
//		Scan(ctx, &v)
func (fadq *FmAlertDefinitionQuery) Select(fields ...string) *FmAlertDefinitionSelect {
	fadq.ctx.Fields = append(fadq.ctx.Fields, fields...)
	sbuild := &FmAlertDefinitionSelect{FmAlertDefinitionQuery: fadq}
	sbuild.label = fmalertdefinition.Label
	sbuild.flds, sbuild.scan = &fadq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a FmAlertDefinitionSelect configured with the given aggregations.
func (fadq *FmAlertDefinitionQuery) Aggregate(fns ...AggregateFunc) *FmAlertDefinitionSelect {
	return fadq.Select().Aggregate(fns...)
}

func (fadq *FmAlertDefinitionQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range fadq.inters {
		if inter == nil {
			return fmt.Errorf("genclients: uninitialized interceptor (forgotten import genclients/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, fadq); err != nil {
				return err
			}
		}
	}
	for _, f := range fadq.ctx.Fields {
		if !fmalertdefinition.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("genclients: invalid field %q for query", f)}
		}
	}
	if fadq.path != nil {
		prev, err := fadq.path(ctx)
		if err != nil {
			return err
		}
		fadq.sql = prev
	}
	return nil
}

func (fadq *FmAlertDefinitionQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*FmAlertDefinition, error) {
	var (
		nodes = []*FmAlertDefinition{}
		_spec = fadq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*FmAlertDefinition).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &FmAlertDefinition{config: fadq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, fadq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (fadq *FmAlertDefinitionQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := fadq.querySpec()
	_spec.Node.Columns = fadq.ctx.Fields
	if len(fadq.ctx.Fields) > 0 {
		_spec.Unique = fadq.ctx.Unique != nil && *fadq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, fadq.driver, _spec)
}

func (fadq *FmAlertDefinitionQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(fmalertdefinition.Table, fmalertdefinition.Columns, sqlgraph.NewFieldSpec(fmalertdefinition.FieldID, field.TypeInt64))
	_spec.From = fadq.sql
	if unique := fadq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if fadq.path != nil {
		_spec.Unique = true
	}
	if fields := fadq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, fmalertdefinition.FieldID)
		for i := range fields {
			if fields[i] != fmalertdefinition.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := fadq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := fadq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := fadq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := fadq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (fadq *FmAlertDefinitionQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(fadq.driver.Dialect())
	t1 := builder.Table(fmalertdefinition.Table)
	columns := fadq.ctx.Fields
	if len(columns) == 0 {
		columns = fmalertdefinition.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if fadq.sql != nil {
		selector = fadq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if fadq.ctx.Unique != nil && *fadq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range fadq.predicates {
		p(selector)
	}
	for _, p := range fadq.order {
		p(selector)
	}
	if offset := fadq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := fadq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// FmAlertDefinitionGroupBy is the group-by builder for FmAlertDefinition entities.
type FmAlertDefinitionGroupBy struct {
	selector
	build *FmAlertDefinitionQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (fadgb *FmAlertDefinitionGroupBy) Aggregate(fns ...AggregateFunc) *FmAlertDefinitionGroupBy {
	fadgb.fns = append(fadgb.fns, fns...)
	return fadgb
}

// Scan applies the selector query and scans the result into the given value.
func (fadgb *FmAlertDefinitionGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, fadgb.build.ctx, ent.OpQueryGroupBy)
	if err := fadgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*FmAlertDefinitionQuery, *FmAlertDefinitionGroupBy](ctx, fadgb.build, fadgb, fadgb.build.inters, v)
}

func (fadgb *FmAlertDefinitionGroupBy) sqlScan(ctx context.Context, root *FmAlertDefinitionQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(fadgb.fns))
	for _, fn := range fadgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*fadgb.flds)+len(fadgb.fns))
		for _, f := range *fadgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*fadgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := fadgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// FmAlertDefinitionSelect is the builder for selecting fields of FmAlertDefinition entities.
type FmAlertDefinitionSelect struct {
	*FmAlertDefinitionQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (fads *FmAlertDefinitionSelect) Aggregate(fns ...AggregateFunc) *FmAlertDefinitionSelect {
	fads.fns = append(fads.fns, fns...)
	return fads
}

// Scan applies the selector query and scans the result into the given value.
func (fads *FmAlertDefinitionSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, fads.ctx, ent.OpQuerySelect)
	if err := fads.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*FmAlertDefinitionQuery, *FmAlertDefinitionSelect](ctx, fads.FmAlertDefinitionQuery, fads, fads.inters, v)
}

func (fads *FmAlertDefinitionSelect) sqlScan(ctx context.Context, root *FmAlertDefinitionQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(fads.fns))
	for _, fn := range fads.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*fads.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := fads.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
